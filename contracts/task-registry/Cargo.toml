[package]
name = "task-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = "26.0.1"
ecotask-types = { path = "../ecotask-types" }

[dev-dependencies]
soroban-sdk = { version = "26.0.1", features = ["testutils"] }
